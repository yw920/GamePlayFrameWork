local interface =
    INTERFACE(
    {
        --@type playerId string const
        playerId = nil,
        --@type setPlayerId function
        setPlayerId = nil
    }
)

return {interface = interface}
