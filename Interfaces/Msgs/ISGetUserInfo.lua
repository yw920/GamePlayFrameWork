local interface =
    INTERFACE(
    {
        --@type userName string const
        userName = nil,
        --@type getUserName function const
        getUserName = nil
    }
)

return {interface = interface}
